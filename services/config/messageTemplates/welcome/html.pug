doctype html
html(lang="en")
body
    - let salutation = user.firstName ? " " + user.firstName : "";
    h2 Hi#{salutation},
    a(href=webserver + "/verify/" + urlParamsObj.token) Click here to verify your registration.