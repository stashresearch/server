doctype html
html(lang="en")
body
    - let salutation = user.firstName ? " " + user.firstName : "";
    h2 Hi#{salutation},
    p There is authentication problem with #{urlParamsObj.provider}. Reason: #{urlParamsObj.reason}